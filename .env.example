### Copy this file to `.env` and fill in your credentials. Do NOT commit `.env` to git.
# Example .env for idx-scraper
# IDX auth email and password (used by --login or --persist-login)
IDX_AUTH_EMAIL=you@example.com
IDX_AUTH_PASSWORD=yourpassword

# Optional: an auth token (Bearer ...) to inject into localStorage/requests
# IDX_AUTH_TOKEN=Bearer xxxxx

# Optional HTTP proxy for requests and Playwright (host:port or full URL)
# IDX_PROXY=http://user:pass@proxy.example.com:3128
